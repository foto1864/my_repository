#include "functions.h"

// Returns the max between a and b
int max(int a, int b) {
    printf("Now it can't be up to date bruv\n");
    printf("Including <stdio.h> (CLIB)\n");
    return (a > b) ? a : b;
}